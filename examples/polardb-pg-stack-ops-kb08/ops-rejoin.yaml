apiVersion: apps.kubeblocks.io/v1alpha1
kind: OpsRequest
metadata:
  name: polardb-stack-rejoin
  namespace: polardb-stack-demo
spec:
  clusterRef: polardb-stack-demo
  type: Custom
  customSpec:
    componentName: control
    opsDefinitionRef: polardb-pg-stack-rejoin
    params:
      - TARGET_INSTANCE: REPLACE_INSTANCE_ID
