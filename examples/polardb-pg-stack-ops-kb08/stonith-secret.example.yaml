apiVersion: v1
kind: Secret
metadata:
  name: polardb-pg-stonith
  namespace: polardb-stack-demo
type: Opaque
stringData:
  # The endpoint must return 2xx only after power-off, network isolation, or
  # shared-storage write access revocation is confirmed by infrastructure.
  endpoint: https://REPLACE_STONITH_PROVIDER/fence
  token: REPLACE_PROVIDER_TOKEN
