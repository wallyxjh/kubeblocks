apiVersion: v1
kind: Namespace
metadata:
  name: polardb-stack-demo
---
apiVersion: apps.kubeblocks.io/v1alpha1
kind: Cluster
metadata:
  name: polardb-stack-demo
  namespace: polardb-stack-demo
  annotations:
    # These names point to an MPDCluster created and owned by the official
    # PolarDB Stack Operator. KubeBlocks never creates or reconciles it.
    polardb-pg.kubeblocks.io/mpdcluster: REPLACE_MPDCLUSTER_NAME
    polardb-pg.kubeblocks.io/mpdcluster-namespace: REPLACE_MPDCLUSTER_NAMESPACE
    polardb-pg.kubeblocks.io/control-plane: stack-operator
spec:
  clusterDefinitionRef: polardb-pg-stack-kb08
  # This Cluster is an audit and operations projection. Do not let its deletion
  # issue a deletion request to a separately owned MPDCluster.
  terminationPolicy: DoNotTerminate
  componentSpecs:
    - name: control
      componentDef: polardb-pg-stack-v1
      replicas: 0
      serviceAccountName: polardb-pg-stack-ops
