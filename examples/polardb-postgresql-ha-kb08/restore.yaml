apiVersion: apps.kubeblocks.io/v1alpha1
kind: OpsRequest
metadata:
  name: pg-ha-restore
  namespace: polardb-pg-ha-kb08
spec:
  clusterRef: pg-ha-restore
  type: Restore
  restoreSpec:
    backupName: pg-ha-backup
    volumeRestorePolicy: Serial
