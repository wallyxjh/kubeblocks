# API-contract test only. A test harness removes restartIns after observing it.
apiVersion: apps.kubeblocks.io/v1alpha1
kind: OpsRequest
metadata:
  name: polardb-stack-contract-rejoin
  namespace: polardb-stack-contract-test
spec:
  clusterName: polardb-stack-contract-test
  type: Custom
  timeoutSeconds: 180
  custom:
    opsDefinitionName: polardb-pg-stack-rejoin
    serviceAccountName: polardb-pg-stack-ops
    components:
      - componentName: control
        parameters:
          - name: TARGET_INSTANCE
            value: rw-0
